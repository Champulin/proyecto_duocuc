import { FacultyListPipe } from './faculty-list.pipe';

describe('FacultyListPipe', () => {
  it('create an instance', () => {
    const pipe = new FacultyListPipe();
    expect(pipe).toBeTruthy();
  });
});
