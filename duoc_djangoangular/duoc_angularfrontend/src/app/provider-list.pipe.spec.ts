import { ProviderListPipe } from './provider-list.pipe';

describe('ProviderListPipe', () => {
  it('create an instance', () => {
    const pipe = new ProviderListPipe();
    expect(pipe).toBeTruthy();
  });
});
